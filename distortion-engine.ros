#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '(:map-distort-engine) :silent t)
  )

(defpackage :ros.script.distortion-engine.3934512586
  (:use :cl))
(in-package :ros.script.distortion-engine.3934512586)

(defun main (&rest argv)
  (declare (ignorable argv))
  (map-distort-engine.ofx::ofx-shaker))
;;; vim: set ft=lisp lisp:
